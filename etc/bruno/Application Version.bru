meta {
  name: Application Version
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/rest/admin/application-version
  body: none
  auth: none
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
